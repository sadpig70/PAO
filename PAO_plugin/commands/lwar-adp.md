---
description: Start (or resume) the PAO ADP watch/execute loop with an approved LWAR identity
---

Load the `pao:lwar-runtime` skill and start the ADP loop with the approved identity file, following the skill's event contract exactly. Only `shutdown` stops the loop.
