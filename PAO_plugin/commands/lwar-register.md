---
description: Register this runtime session as a PAO LWAR and prepare the ADP loop
argument-hint: "[number]"
---

Load the `pao:lwar-runtime` skill and follow its registration contract. Requested slot: $ARGUMENTS (automatic lowest slot if empty).

Register with the real metadata of this runtime, wait for OA approval, adopt the returned identity, then start the ADP watch loop per the skill.
